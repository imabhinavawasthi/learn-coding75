# CSS HEX Colors

A **hexadecimal color** is specified with: `#RRGGBB`, where the **RR** (red), **GG** (green), and **BB** (blue) hexadecimal integers specify the components of the color.

## HEX Value

In CSS, a color can be specified using a hexadecimal value in the form:

```css
#rrggbb
```
## HEX Color Preview

<div style={{ backgroundColor: '#ff0000', color: 'white', padding: '10px', marginBottom: '8px' }}>
  #ff0000
</div>
<div style={{ backgroundColor: '#0000ff', color: 'white', padding: '10px', marginBottom: '8px' }}>
  #0000ff
</div>
<div style={{ backgroundColor: '#3cb371', color: 'white', padding: '10px', marginBottom: '8px' }}>
  #3cb371
</div>
<div style={{ backgroundColor: '#ee82ee', color: 'black', padding: '10px', marginBottom: '8px' }}>
  #ee82ee
</div>
<div style={{ backgroundColor: '#ffa500', color: 'black', padding: '10px', marginBottom: '8px' }}>
  #ffa500
</div>
<div style={{ backgroundColor: '#6a5acd', color: 'white', padding: '10px', marginBottom: '8px' }}>
  #6a5acd
</div>

# 3-Digit HEX Value

Sometimes you will see a **3-digit hex code** in the CSS source.

The 3-digit hex code is a shorthand for some 6-digit hex codes.

The 3-digit hex code has the following form:

```css
#rgb
```

## CSS 3-Digit HEX Example

```css
body {
  background-color: #fc9; /* same as #ffcc99 */
}

h1 {
  color: #f0f; /* same as #ff00ff */
}

p {
  color: #b58; /* same as #bb5588 */
}
