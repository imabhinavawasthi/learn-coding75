---
title: "JavaScript Math Reference"
date: "26 May, 2023"
---

JavaScript `Math` object is used to perform mathematical operations on numbers. `Math` is an inbuilt object that works with `Number` types but does not work with `BigInt`.

## Example

Below example will give you a brief idea of JavaScript math objects:

```javascript
// Return PI value (3.141592653589793) 
console.log(Math.PI);
```

### Output:
```
3.141592653589793
```

This will print the value of Pi on the console.

## JavaScript Math Properties and Methods

JavaScript Math object properties and methods in JavaScript are listed below:

### JavaScript Math Properties

A JavaScript property is a member of an object that associates a key with a value. In the `Math` object of JavaScript, there is a single type of property: static properties. No instance properties are present.

#### Static Property

A static property is a property that has the same value for the entire class.